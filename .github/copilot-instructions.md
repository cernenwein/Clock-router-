# ClockRouter repository instructions

Read and follow `/AGENTS.md`; it is the canonical agent contract. Then read
`/docs/CONSTITUTION.md` and the active numbered folder under `/specs` before
editing production code. Do not implement non-trivial changes without a spec,
plan, and task entry. Privacy constraints fail closed. Run the verification
commands required by `AGENTS.md` and record evidence in the active spec.
